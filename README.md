# SW_ETL

## run locally

- install docker
- install docker-compose

for the first time
```bash
docker-compose up --build
```

next run this
```bash
docker-compose up
```

## tests

```bash
docker-compose run sw_etl pytest
```
